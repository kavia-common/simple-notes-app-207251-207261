# Optional: Base URL for the notes backend API (FastAPI).
# If not set, the frontend will use same-origin requests.
# Example for local dev:
# REACT_APP_NOTES_API_URL=http://localhost:3001
REACT_APP_NOTES_API_URL=
